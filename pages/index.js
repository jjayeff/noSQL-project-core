import Layout from '../components/Layout'
import Admin from '../components/admin/info'

const Index = (props) => (
  <Layout title='แอดมิน - ข้อมูลหนังทั้งหมด'  type='admin'>
    <Admin title='ข้อมูลหนังทั้งหมด'/>
  </Layout>
)

export default Index